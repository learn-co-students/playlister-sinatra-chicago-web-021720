<% if flash.has?(:message) %>
  <h4><%= flash[:message] %></h4>
<% end %>

<h2>Songs</h2>
<ul>
<% @songs.each do |song| %>
  <li><a href="/songs/<%= song.slug %>"><%= song.name %> - <%= song.artist.name %></a></li>
<% end %>
</ul>
